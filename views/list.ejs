<%- include('header') ;-%>
    <div class="box">
        <h1 id="heading"><%= ListTitle %></h1>
    </div>
    <div class="box">
        <% for(var i= 0; i<newListItem.length; i++){ %>
            <div class="item">
                <input type="checkbox" >
                <p><%=newListItem[i]%></p>
            </div>
            <%}%>
            <form action="/" method="POST" class="item">
                <input type="text" name="newItem" placeholder="Wahts do you want ot add">
                <button type="submit" name="buttonPost" value="<%= ListTitle %>">+</button>
            </form>
        
    </div>
<%- include('footer');  -%>