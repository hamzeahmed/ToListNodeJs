<%- include('header') ;-%>
    <div class="box">
        <h1 id="heading"><%= ListTitle %></h1>
    </div>
    <div class="box">
        <% newListItem.forEach((item)=>{ %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkedItem" value="<%=item._id%>" onclick="this.form.submit()">
                    <p><%=item.name%></p>
                </div>
                <input type="hidden" name="hiddenInput" value="<%= ListTitle%>">
            </form>
        <% }); %>
            <form action="/" method="POST" class="item">
                <input type="text" name="newItem" placeholder="Wahts do you want ot add">
                <button type="submit" name="list" value="<%=ListTitle%>">+</button>
            </form>
        
    </div>
<%- include('footer');  -%>